<h1><%= @user.name %></h1>
<% if user_signed_in? && current_user.id == @user.id %>
  <%= link_to "Edit", "#{users_path}/#{@user.id}/edit" %>
<% end %>
<% if @user.info %>
  <p><%= markdown(@user.info) %></p>
<% end %>
